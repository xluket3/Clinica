@model IEnumerable<ClinicaSonrrisaPlena.Models.Entities.PlanTratamiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaCreacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ObservacionesGenerales)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paciente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Odontologo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCreacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ObservacionesGenerales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paciente.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Odontologo.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPlan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPlan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPlan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
